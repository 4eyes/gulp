'use strict';

var _ = require(modulesPath + 'lodash'),
    requireNew = require(modulesPath + 'require-new')
    ;

var data = {
    id: 'input',
    type: 'text',
    name: 'email',
    label: 'E-Mail'
};

module.exports = data;