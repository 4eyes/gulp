'use strict';

var _ = require(modulesPath + 'lodash'),
    requireNew = require(modulesPath + 'import-fresh')
    ;

var data = {
    centered: true,
    headline: 'Headline',
    header: 'Überschrift'
};

module.exports = data;