'use strict';

var _ = require(modulesPath + 'lodash'),
    requireNew = require(modulesPath + 'require-new')
    ;

var data = {
    centered: true,
    headline: 'Headline',
    header: 'Überschrift'
};

module.exports = data;